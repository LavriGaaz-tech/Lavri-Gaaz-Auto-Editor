# Lavri_Gaaz Auto Editor — Deploy Ready

This package is prepared for container deployment. It exposes Flask on 0.0.0.0 and uses the PORT environment variable. FFmpeg is installed in the image. Output can be persisted by setting OUTPUT_DIR to a persistent mounted path.
