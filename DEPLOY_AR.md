# Lavri_Gaaz Auto Editor — نشر مباشر

هذه النسخة مجهزة للنشر على Render باستخدام Docker.

## مهم
الخطة المجانية مناسبة للاختبار فقط. Render يوقف الخدمة المجانية بعد 15 دقيقة من عدم النشاط، ونظام الملفات فيها مؤقت. معالجة فيديوهات طويلة تحتاج عادةً خدمة مدفوعة/عامل خلفي وتخزين دائم.

## النشر
1. ارفع هذا المشروع إلى مستودع GitHub.
2. في Render اختر New → Web Service.
3. اربط مستودع GitHub.
4. Render يكتشف Dockerfile تلقائيًا.
5. أنشئ الخدمة.
6. ستحصل على رابط `onrender.com` عام.

بعد النشر افتح الرابط، والصق رابط YouTube واضغط بدء المعالجة.
